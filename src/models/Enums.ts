export enum Privacity {
  PUBLIC = 'public',
  PRIVATE = 'private',
}

export enum FollowRequestStatus {
  PENDING = 'pending',
  APPROVED = 'approved',
  REJECTED = 'rejected',
}

export enum FollowRequestProcessingAction {
  ACCEPT = 'accept',
  REJECT = 'reject',
}
